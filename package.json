{
  "name": "punchcard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Paper punch time-card reader: static app + a thin self-hosted Gemma 4 vision proxy.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test"
  }
}
